# ctrl: control
# windows key: super
# shift: shift
# alt: alt
# see xev

#BRIGHTNESS
{XF86MonBrightnessUp,XF86MonBrightnessDown}
	{brightnessctl -q -e2 -n10 s 5%+ && pkill -RTMIN+2 dwmblocks,brightnessctl -q -e2 -n10 s 5%- && pkill -RTMIN+2 dwmblocks}

#VOLUME
{XF86AudioRaiseVolume,XF86AudioLowerVolume,XF86AudioMute}
	{pamixer -i 2 && pkill -RTMIN+5 dwmblocks,pamixer -d 2 && pkill -RTMIN+5 dwmblocks,pamixer -t && pkill -RTMIN+5 dwmblocks}
#{XF86AudioRaiseVolume,XF86AudioLowerVolume,XF86AudioMute,XF86AudioMicMute}
	#{wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+ && pkill -RTMIN+5 dwmblocks,\
	# wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%- && pkill -RTMIN+5 dwmblocks,\
	# wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && pkill -RTMIN+5 dwmblocks,\
	# wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle && pkill -RTMIN+5 dwmblocks}

#BROWSER
super + w
	$BROWSER

#FILE MANAGER
super + e
	$TERMINAL -e lfub

#EMOJIS
super + alt + e
	dmenu_emojis.sh

#ICONS
super + alt + i
	dmenu_icons.sh

#SET BACKGROUND
super + alt + b
	dmenu_bg.sh
#SET RANDOM BACKGROUND
super + alt + w
	random_bg.sh

#CLIPBOARD SHOW
#super + alt + v
super + v
	clipmenu
#CLIPBOARD CLEAR
#super + alt + v
super + shift + v
	clipdel -d '.*'

#TERMINAL
super + Return
	$TERMINAL

#POWEROFF
super + alt + x
	dmenu_poweroff.sh

#LOCKSCREEN
super + alt + l
	slock

#SCREEN SHOT
{super + Print,Print}
	{flameshot screen,flameshot gui}

#DWM EXIT
super + shift + q
	loginctl -s 15 --no-ask-password terminate-session ${XDG_SESSION_ID}
